export interface BasicControls {
  style?: object;
  context: any;
  settings: any;
}

//materialControl interface
export interface MaterialControlsProps {
  materialArray: [];
  materialDimensions: {};
}
